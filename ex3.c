#include <stdio.h>

int main(){
	
void media(int soma, float media, int tamanho) {
    // Função que realiza alguma ação com os números fornecidos
    soma = 0;
    int tamanho = sizeof(numero) / sizeof(numero[6]);
    for(int i = 0; i < tamanho; i++) {
          printf("A soma dos numeros é: %d\n", soma);
    }
    printf("\n");
}
	
	int i, numero[6];
	
    printf("Escolha 6 números:\n");
    for (i = 0; i < 6; i++) {
        printf("Numero %d: ", i + 1);
        scanf("%d", &numero[i]);
    }
	
	
	media(numero, 6);
	
	
}
